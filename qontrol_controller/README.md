# Qontrol_controller (INRIA)

This package was developed by INRIA and thus, is under restricted access for now so it's not imported directly as submodule but if you have access, execute the following command to clone the project:

```bash
## From home folder 'explorer_ws'
make init -C qontrol_controller
OR
## From package folder 'qontrol_controller'
make init
```